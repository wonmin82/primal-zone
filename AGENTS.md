# 원시구역 프로젝트 작업 가이드

이 문서는 저장소 전체에 적용하는 작업 지침이다. 사용자의 명시적인 요청을 우선하며, 더 구체적인 하위 경로 지침이 있으면 함께 확인한다.
응답, 개발 문서, 커밋 제목과 본문은 한국어로 작성한다. 코드 식별자, 명령어, 파일명, 제품명과 Git 유형 표기는 원문을 사용할 수 있다.

## 1. 프로젝트 방향

- 원시구역(Primal Zone)은 사냥·장비 수집·성장을 중심으로 하는 한국어 웹 MUD다.
- 사냥·장비·성장을 우선하고, 탐험·임무가 다음 사냥터와 성장 동기를 제공하도록 만든다.
- 주요 임무와 보스는 혼자 진행할 수 있어야 한다. 이용자 간 상호작용은 선택적으로 제공한다.
- 현재 기반은 Python 3.13, Evennia, SQLite와 HTML·CSS·JavaScript다. 정확한 의존성 버전은 `pyproject.toml`과 `uv.lock`을 기준으로 한다.
- 새로운 기능의 범위를 판단할 때 [README](README.md), [구조와 설계 결정](docs/architecture.md), [테스트 안내](docs/playtest.md)를 참고한다.

## 2. 작업 시작과 변경 범위

1. 현재 작업 디렉터리, 브랜치, `git status --short`를 확인한다. 같은 프로젝트의 다른 복사본과 혼동하지 않는다.
2. 요청과 관련된 코드·문서·기존 테스트를 읽고 실제 동작을 확인한다. 문서의 설명만으로 구현 완료를 단정하지 않는다.
3. 기존 미커밋 변경을 보존한다. 작업과 관계없는 수정·포맷 변경·파일 정리를 섞지 않는다.
4. 요청된 동작을 완성하는 데 필요한 범위로 수정한다. 새 의존성은 기존 구조로 해결하기 어려운 이유가 있을 때 추가한다.
5. 조작 방법, 게임 규칙, 실행 명령을 바꿨다면 관련 문서와 테스트의 기대 결과도 함께 갱신한다.

DB·비밀 설정·로그·가상환경·수집된 정적 파일은 소스 변경에 포함하지 않는다.
플레이 DB를 테스트 초기화 수단으로 삭제하거나 덮어쓰지 않는다.

## 3. 구현 시 지킬 기준

| 위치 | 역할과 변경 기준 |
| --- | --- |
| `game/world/content/` | 지역별 장소·적과 공통 아이템·가격 데이터. 저장에 사용하는 ID를 바꿀 때는 기존 진행 기록의 호환성을 검토한다. |
| `game/world/rules.py` | 전투·성장·보상 계산. Evennia, DB, 네트워크에 의존하지 않는 규칙 계층으로 유지한다. |
| `game/typeclasses/explorers.py` | 플레이어 상태 저장, 전투 타이머, 화면 상태 전송. 중복 타이머와 중복 보상을 방지한다. |
| `game/commands/` | 한국어 명령과 입력 검증. 명령과 웹 버튼이 같은 서버 규칙을 사용하도록 한다. |
| `game/world/bootstrap.py` | 월드 구성. 반복 실행해도 장소·출구를 중복 생성하거나 플레이 기록을 초기화하지 않아야 한다. |
| `game/web/` | 웹 화면과 입력. 전투 결과와 보상은 서버에서 결정하고 클라이언트는 요청과 표시를 담당한다. |

- 공유 적의 HP, 파티의 단일 상태, 참여 기반 보상과 전리품 보호 권한을 보존한다. 공동 전투에서도 기존 솔로 진행을 유지한다.
- 공격 명령 반복으로 전투 속도가 빨라지지 않도록 서버 타이머를 기준으로 처리한다.
- 패배·보상·임무 완료·구매 실패 시 상태가 일부만 저장되거나 보상이 중복 지급되지 않도록 한다.
- 저장 구조를 바꿀 때는 기존 캐릭터의 로드, 기본값, 변환 방법과 재접속 동작을 확인한다.
- 웹 화면 변경 시 4절의 영향 범위에 따라 한글 조합 입력, 버튼과 명령의 동등한 동작, 좁은 화면에서의 조작을 확인한다.
- 가입·로그인 정보를 게임 기록이나 명령어 기록에 노출하지 않는다.

## 4. 검증과 결과 보고

명령은 저장소 루트에서 실행한다. 실행 환경이 구성된 Windows에서는 다음 명령을 사용한다.

```powershell
.\.venv\Scripts\python.exe scripts/dev.py check
.\.venv\Scripts\python.exe scripts/dev.py test
```

uv 환경에서는 각각 `uv run python scripts/dev.py check`, `uv run python scripts/dev.py test`로 실행할 수 있다.

### 변경에 따른 검사 선택

테스트는 작업 단계나 커밋 횟수가 아니라 실제 변경의 영향으로 결정한다. 사용자가 명시한 검증 요구는 이 기본 기준보다 우선한다.

| 변경 또는 상황 | 실행할 검사 |
| --- | --- |
| 게임 규칙·명령·저장 로직 수정 | 개발 중에는 직접 영향을 받는 규칙·통합 테스트와 코드 검사를 실행한다. 필요한 회귀 테스트를 보완한다. |
| 공통 규칙·parser·공유 전투·저장 변환, 의존성·테스트 설정 등 넓은 영향의 변경 | 관련 테스트를 먼저 통과시킨 뒤 코드가 정리된 시점에 전체 `scripts/dev.py check`와 `scripts/dev.py test`를 실행한다. 영향 범위를 좁히기 어렵거나 회귀 우려가 남은 경우도 포함한다. |
| 실패 수정 | 실패한 테스트와 수정 영향 범위를 먼저 재검증한다. 실패할 때마다 전체 테스트부터 반복하지 않는다. |
| 웹 화면·CSS·JavaScript 수정 | 영향을 받는 화면·조작을 브라우저에서 확인한다. JavaScript 변경 시 `node --check game/web/static/webclient/js/primal.js`를 실행한다. 서버 코드에 영향이 없다면 전체 Python 테스트는 기본적으로 생략한다. |
| 문서만 수정 | 명령·경로·링크·내용의 정확성과 `git diff --check`를 확인한다. 게임 테스트, 브라우저, smoke, 정적 파일 수집은 실행하지 않는다. |
| 대상 브랜치 갱신·rebase·충돌 해결 | 이전 검증 이후의 diff와 의존성 영향을 평가하고 관련 검사를 실행한다. 넓은 영향이 있을 때만 전체 테스트로 확대한다. |
| 커밋·푸시·PR 본문 수정·병합 준비 | 이 작업 자체는 로컬 테스트 재실행 조건이 아니다. 검증 이후의 변경과 최신 CI 상태를 확인한다. |

### 실행 범위와 중복 방지

- 전체 테스트가 필요한 작업은 최종 코드가 정리된 시점의 성공 결과 한 번을 기본으로 한다. 이후 관련 코드·의존성·설정 변화나 새로운 실패가 없다면 같은 검사를 반복하지 않는다.
- 최종 변경 검증은 마지막에 무조건 전체 테스트를 다시 실행한다는 뜻이 아니다. 마지막 성공 이후의 diff를 검토하고 아직 검증되지 않은 영향만 검사한다. 예를 들어 Python 테스트 통과 후 문서와 CSS만 바뀌었다면 문서 검사와 해당 화면 확인을 수행한다.
- 브라우저는 변경된 화면을 중심으로 확인한다. 공통 CSS·레이아웃 변경은 데스크톱과 좁은 화면의 대표 화면으로, 입력·명령 전달 변경은 한글 조합 입력과 버튼·직접 명령의 동등한 동작으로 범위를 확대한다. 실제 OS IME 미검증을 자동 테스트 통과로 대신하지 않는다.
- CSS·JavaScript·글꼴 변경은 최종 정적 파일을 `game` 디렉터리에서 수집한 뒤 브라우저로 확인한다. 후속 정적 파일 변경이 없다면 수집을 반복하지 않는다.
- 테스트는 실제 회귀나 중요한 경계를 검증하도록 작성한다. 구현을 그대로 따라 쓰거나 사소한 문구 변경만 검증하는 테스트를 추가하지 않는다.
- 논리적으로 묶인 수정과 검증을 마친 뒤 푸시한다. 작은 수정마다 푸시하여 CI를 불필요하게 반복하지 않는다. 필수 CI와 보호 규칙은 우회하지 않는다.

### 검증 근거와 보고

- 실행 명령, 성공·실패, 기준 커밋과 미커밋 변경 범위, 필요한 수동 확인 결과를 기록한다.
- 관련 코드가 동일하고 이후 변경이 해당 검증에 영향을 주지 않음을 확인했다면 기존 성공 결과를 근거로 사용할 수 있다. 원래 실행 시점·기준과 재실행하지 않은 이유를 명시하고 이번에 새로 실행한 결과처럼 표현하지 않는다.
- 코드·대상 브랜치가 바뀌면 영향 평가와 리뷰를 갱신한다. 이전 커밋의 CI 성공을 최신 PR HEAD의 CI 결과로 대신하지 않는다.
- 로컬 검사와 CI 결과를 구분한다. 현재 CI는 push와 pull_request에 자동 실행되므로 로컬 중복 실행을 줄이는 기준과 별개로 최신 커밋의 CI를 확인한다.
- 수행하지 않은 검사는 미실행 사유를 적고 통과로 표현하지 않는다.

실행 중인 로컬 서버의 가입·사냥·장비·재접속 흐름은 다음 명령으로 확인한다.

```powershell
.\.venv\Scripts\python.exe scripts/smoke.py
```

이 검사는 실제 로컬 플레이 DB에 일반 테스트 계정 3개를 남긴다. 필요한 변경에 한해 실행하며, 브라우저 화면 검증이나 전체 보스 임무 검증을 대신하지 않는다.
수동 절차는 [테스트 안내](docs/playtest.md), 정적 파일 수집 방법은 [README](README.md#개발)를 따른다.

작업 결과를 설명할 때는 **무엇이 달라졌는지 → 왜 필요한지 → 어떻게 확인했는지 → 남은 제한 사항** 순서로 작성한다.

## 5. Git 작업 원칙

### 작업 브랜치

- 새로운 독립 작업은 원격을 fetch하고 최신 `origin/main`에서 시작한다. PR 대상이 다른 브랜치라면 해당 원격 브랜치를 기준으로 한다.
- 논리적으로 독립된 변경마다 별도의 브랜치와 PR을 사용한다. 새 브랜치에는 별도 요청이 없으면 `codex/` 접두사를 사용한다.
- 기존 PR을 수정하는 요청은 해당 브랜치에서 이어간다. 이미 진행 중인 작업을 새 브랜치 규칙에 맞추기 위해 임의로 분리하거나 초기화하지 않는다.
- `main`에 직접 커밋하거나 푸시하지 않는다.
- 브랜치 전환과 rebase 전에 작업 트리를 확인하고, 관련 없는 미커밋 변경을 보존한다.

### 커밋과 작업 범위

- 커밋 하나는 설명 가능한 하나의 변경 목적을 담는다. 무관한 기능·리팩터링·문서 작업을 한 커밋에 묶지 않는다.
- 커밋할 때는 대상 파일을 명시해 스테이징하고 `git diff --cached`로 실제 포함 내용을 확인한다. 기존 미커밋 변경을 무심코 함께 넣지 않는다.
- 비밀번호·토큰·비밀 설정·개인 플레이 데이터와 불필요한 개인 경로를 커밋이나 커밋 메시지에 넣지 않는다.
- 이 지침이나 메시지 초안의 작성만으로 커밋·푸시·PR 병합을 실행하지 않는다. 실제 실행은 사용자가 요청한 작업 범위를 따른다.

### 최신 대상 브랜치 반영과 이력 보존

- PR의 최종 리뷰 또는 병합 준비 시 원격을 fetch하고, 최신 대상 브랜치 위로 PR 브랜치를 rebase한다. 기본 대상은 `origin/main`이다.
- 이미 최신 대상 커밋을 포함한다면 불필요하게 커밋을 재작성하지 않는다.
- 대상 브랜치를 PR 브랜치에 merge하는 방식으로 필수 rebase를 대신하지 않는다.
- 필수 rebase에서는 기존 커밋의 순서와 메시지를 유지한다. 충돌을 해결할 때는 양쪽 변경의 의도를 확인하고, 변경 누락이나 중복이 없는지 전체 diff를 검토한다.
- 이미 푸시한 커밋이 rebase로 변경되었다면 `git push --force-with-lease`로 갱신한다. 무조건적인 `--force`는 사용하지 않는다.
- 원격 브랜치가 예상과 달라 lease 검사가 실패하면 원격 변경을 먼저 확인한다. 실패를 우회하기 위해 강제 푸시하지 않는다.
- 최종 리뷰·병합 준비에 필요한 rebase 외의 amend, squash, 커밋 순서 변경과 메시지 재작성은 사용자가 명시적으로 요청한 경우에만 수행한다.
- 새 메시지 형식을 적용하기 위해 과거 커밋을 재작성하지 않는다. 이후 작성하는 커밋부터 적용한다.
- 다른 PR에 의존하는 작업은 선행 PR의 rebase·병합 영향을 확인한다. 후속 PR을 갱신할 때는 이미 반영된 변경이 중복 포함되지 않는지 확인한다.

## 6. 커밋 메시지 작성 규칙

### 필수 원칙

**모든 새 커밋 메시지는 한국어 제목과 설명 본문을 함께 작성한다. 제목만 있는 메시지는 사용하지 않는다.**
작은 수정과 문서 변경도 본문을 포함한다. 자세히 쓴다는 것은 문장을 늘리는 것이 아니라, 변경 이유와 결과를 검토할 수 있을 만큼 구체적으로 설명한다는 뜻이다.

- 제목은 변경의 핵심 결과를 한 줄로 요약한다. `수정`, `업데이트`, `버그 수정`처럼 대상과 결과를 알 수 없는 제목을 피한다.
- 제목과 본문 사이에는 빈 줄을 하나 둔다.
- 본문은 `## 변경 배경`, `## 수정 내용`, `## 검증`을 필수로 작성한다.
- 각 섹션은 짧은 문단 또는 `-` 목록으로 작성하고 제목 다음에 빈 줄을 둔다. 변경이 작으면 섹션당 한 항목으로 충분하다.
- 파일명만 나열하지 말고 해당 수정으로 달라지는 동작을 설명한다. 필요한 경우 재현 조건과 수정 전·후를 적는다.
- 최종 diff에 포함된 내용만 설명한다. 대화 과정, 폐기한 접근, 요청과 무관한 작업 내역은 넣지 않는다.
- 검증에는 실제 실행한 명령과 결과를 쓴다. 미실행 항목은 이유를 함께 적는다.
- 영향·호환성·운영 조치·남은 작업이 있으면 `## 영향 및 후속 작업`을 추가한다. 관련 내용이 없으면 이 섹션은 생략한다.
- 긴 구분선, 장식용 이모지, 깊은 중첩 목록과 반복 설명을 피한다.

### 커밋 메시지 길이와 가독성

- 제목은 변경의 핵심 결과를 한 줄로 작성한다. 표시 폭 50칸 이내를 권장하며, 72칸을 넘으면 간결하게 다듬을 수 있는지 검토한다. 필요한 의미를 생략하면서까지 길이를 맞추지는 않는다.
- 제목의 표시 폭에는 유형, 범위, 구두점과 공백을 포함한다. 표시 폭은 참고 기준이며 자동 차단 조건으로 사용하지 않는다.
- 본문에는 72칸 제한을 적용하지 않는다. 문장 중간에 폭을 맞추기 위한 강제 줄바꿈을 넣지 않고, 화면의 자동 줄바꿈을 활용한다.
- 목록은 한 항목에 하나의 변경이나 검증 결과를 담는다. 보통 1~2문장으로 작성하고, 설명이 길어지면 의미에 따라 별도의 항목이나 문단으로 나눈다.
- 하나의 목록 항목이나 문단은 원칙적으로 한 물리적 줄에 작성한다. 구조상 필요한 코드 블록과 하위 목록은 예외다.
- 제목과 본문 사이, 섹션 제목과 내용 사이에는 빈 줄을 둔다.
- URL, 파일 경로, 코드 식별자와 실행 명령은 중간에서 끊지 않는다.
- 길이보다 구체성, 자연스러운 한국어, 중복 없는 설명을 우선한다.
- 본문은 반드시 작성하며 `변경 배경`, `수정 내용`, `검증`의 필수 구조를 유지한다.

### 제목 형식

```text
유형(범위): 한국어 변경 결과
```

유형과 범위는 Git 도구에서 구분하기 위한 표기이므로 영문을 허용한다. 범위는 도움이 될 때만 넣는다.

| 유형 | 용도 | 제목 예시 |
| --- | --- | --- |
| `feat` | 새로운 기능 | `feat(combat): 방어 성공 시 반격 행동 추가` |
| `fix` | 잘못된 동작 수정 | `fix(save): 재접속 시 저장된 적 체력 유지` |
| `docs` | 문서 변경 | `docs: 첫 탐사 테스트 절차와 정상 결과 정리` |
| `refactor` | 동작을 유지하는 구조 개선 | `refactor: 장비 능력치 계산 분리` |
| `test` | 테스트 추가·수정 | `test(quest): 임무 중복 보상 검증 추가` |
| `style` | 동작 변화 없는 코드 형식 변경 | `style: Python import 순서 정리` |
| `chore` | 개발 설정·의존성·도구 관리 | `chore: 개발 의존성과 잠금 파일 갱신` |
| `ci` | 자동 검사 설정 | `ci: PR에서 게임 규칙과 통합 테스트 실행` |

UI의 기능이나 배치를 변경했다면 코드 형식만 바꾸는 `style`과 구분해 변경 목적에 맞는 유형을 선택한다.

### 기본 템플릿

아래 대괄호는 작성 안내다. 실제 커밋에서는 구체적인 내용으로 바꾸고 안내 문구를 남기지 않는다.

```text
유형(범위): 한국어 변경 결과

## 변경 배경

- [어떤 상황에서 무엇이 부족하거나 잘못되었는지와 수정 목적]

## 수정 내용

- [주요 변경과 그로 인해 달라지는 동작]
- [필요한 경우 상태 저장, 예외 처리, 화면 또는 문서에 대한 변경]

## 검증

- [실행한 명령]: [실제 결과]
- [수동 확인 항목]: [실제 결과 또는 미실행 사유]

## 영향 및 후속 작업

- [해당하는 경우 호환성, 데이터 변환, 배포 조치, 미해결 사항]
```

검증 명령이 긴 경우 명령과 결과를 두 줄로 나눠도 된다. 테스트 개수는 실제 출력에서 확인한 경우에만 적는다.
의존성 변경은 중요한 버전 변경을, 저장 형식 변경은 기존 데이터에 대한 영향을 본문에 포함한다.

### 작은 문서 변경 예시

아래는 형식 예시다. 예시의 검증 결과를 복사하지 말고 실제 수행한 결과로 작성한다.

```text
docs: 첫 탐사 테스트 정상 결과 추가

## 변경 배경

- 기존 안내에는 조작 순서만 있어 테스트 성공 여부를 판단하기 어려웠다.

## 수정 내용

- 가입부터 사냥·장비 착용·재접속까지 입력 명령과 기대 결과를 함께 정리했다.
- 확률 드롭과 확정 교환을 구분해 장비 획득 실패를 오류로 오인하지 않도록 했다.

## 검증

- 게임 규칙과 문서의 보상·교환 수치를 대조해 일치함을 확인했다.
- git diff --check: 통과.
- 게임 테스트: 실행 코드 변경이 없어 미실행.
```

### 메시지 전달과 최종 확인

여러 줄의 메시지는 UTF-8 텍스트 파일로 작성하고 `git commit -F <메시지 파일>`로 전달하는 방식을 권장한다.
메시지 파일은 커밋 대상에 포함하지 않는다. 실제 줄바꿈을 사용하고 문자 그대로의 `\n`이 본문에 남지 않도록 한다.

커밋이 요청된 작업에서는 실행 전에 다음 항목을 확인한다.

- 제목과 본문이 한국어이며 변경 대상과 결과가 구체적인가?
- 제목이 간결하고, 본문이 의미 단위로 구성되어 있으며 불필요한 강제 줄바꿈이 없는가?
- 필수 세 섹션이 있고, 본문이 최종 스테이징 내용과 일치하는가?
- 검증 결과는 실제 수행한 내용인가? 미실행 사유와 필요한 후속 조치가 명확한가?
- 하나의 목적에 해당하는 파일만 포함되어 있고 민감한 정보가 없는가?

커밋 후에는 `git log -1 --format=full`로 제목·본문·줄바꿈이 의도대로 저장되었는지 확인한다.

## 7. PR 작성·리뷰·병합

PR을 작성하거나 수정하는 작업에서는 한국어 제목과 본문을 사용한다.
본문은 `Summary`, `Changes`, `Validation` 섹션으로 구성하고 각 섹션의 설명을 한국어로 작성한다.
문제와 변경 후 동작, 주요 수정, 실제 검증 결과 및 필요한 제한 사항을 적는다.
PR 제목과 본문은 최종 변경 범위에 맞추며, 커밋 메시지를 그대로 이어 붙이는 방식은 피한다.

### PR 생성과 갱신

- PR은 사용자가 요청했거나 합의된 작업 범위에 포함된 경우 생성한다.
- PR 생성 전 대상 브랜치 대비 전체 diff를 검토하고 4절에 따라 관련 검증 근거를 확인한다. 이미 검증한 동일 코드의 로컬 검사를 PR 생성만을 이유로 반복하지 않는다.
- PR이 변경되면 제목·본문·검증 결과를 최종 범위에 맞춰 갱신한다. 미검증 동작과 남은 제한 사항을 명시한다.
- 푸시 후 최신 커밋의 CI 결과를 확인한다. 실패한 검사는 원인을 확인하고, 요청 범위의 문제를 수정한 뒤 재검증한다.

### 최종 리뷰와 병합

- 최종 리뷰에서는 정확성, 회귀, 입력 검증, 권한, 저장 데이터 호환성, 오류 처리와 의도하지 않은 동작 변경을 확인한다.
- 이 프로젝트에서는 중복 전투 타이머·보상, 점유·참여 보상·전리품 배정의 정확성, 재접속과 생명주기 복원, 명령과 웹 버튼의 동등한 동작을 중점적으로 검토한다.
- 리뷰 지적은 코드 위치나 재현 결과를 근거로 작성하고, 발생 조건·영향·수정 방향을 설명한다. 확인된 문제와 추정·미검증 사항을 구분한다.
- 병합 직전에 다음 조건을 확인한다.
  - PR이 Draft가 아니며 충돌 없이 병합 가능한가?
  - 최신 대상 브랜치가 반영되어 있는가?
  - 최신 PR 커밋의 필수 CI 검사와 필요한 리뷰 승인이 완료되었는가?
  - 미해결 리뷰 대화와 병합을 막는 문제가 없는가?
  - PR 본문의 변경 범위와 검증 결과가 현재 커밋과 일치하는가?
- 리뷰 대화는 실제 수정 또는 근거 있는 설명으로 처리한다. 병합 조건을 맞추기 위해 미해결 문제를 형식적으로 해결 처리하지 않는다.
- 병합은 사용자가 명시적으로 요청한 경우에만 수행한다. PR 작성·수정·리뷰·병합 준비 요청만으로 병합하지 않는다.
- 사용자가 이미 해당 PR의 병합을 요청했고 조건이 충족되었다면 같은 승인을 다시 요청하지 않는다.
- 별도 요청이 없으면 merge commit 방식으로 병합한다. 병합 전 rebase와 GitHub의 rebase merge는 서로 다른 절차다.
- 병합 조건이 충족되지 않으면 보호 규칙을 우회하지 않고 원인과 필요한 조치를 보고한다.

### 병합 후 정리

- 병합 완료와 merge commit을 확인하고, 병합된 대상 브랜치의 CI 결과를 확인한다.
- 소스 브랜치는 사용자가 삭제를 요청한 경우에만 삭제한다. 삭제 전에 변경사항이 대상 브랜치에 반영되었는지 확인한다.
- 로컬 브랜치를 정리할 때는 미커밋 변경과 다른 worktree의 사용 여부를 확인한다.
- 로컬 대상 브랜치는 가능하면 fast-forward로 최신화한다. 로컬 고유 커밋이나 변경을 강제로 덮어쓰지 않는다.
- 최종 보고에는 PR 링크, 병합 커밋, 검증 결과, 브랜치 삭제 여부와 남은 제한 사항을 기록한다.
